<section id="top" class="hero">
	<div class="container hero-grid">
		<div class="hero-copy">
			<p class="eyebrow">Instant mowing quotes</p>
			<h1 class="display">Quote mowing jobs in a few seconds.</h1>
			<p class="subtitle">
				Enter an address. Get a weekly + bi-weekly price based on property square footage and your
				pricing formula.
			</p>
			<div class="hero-actions">
				<a class="button primary" href="#get-updates">Get Updates</a>
				<a class="button secondary" href="#how-it-works">See how it works</a>
			</div>
			<div class="hero-metrics">
				<div>
					<span class="metric-label">Average response</span>
					<strong>Under 2 minutes</strong>
				</div>
				<div>
					<span class="metric-label">Quote styles</span>
					<strong>Weekly + bi-weekly</strong>
				</div>
			</div>
		</div>
		<div class="hero-card">
			<h2 class="card-title">Today’s workflow</h2>
			<ul>
				<li>Client texts you a street address</li>
				<li>You measure the lot in a mapping tool</li>
				<li>You calculate the price + reply later</li>
			</ul>
			<div class="divider"></div>
			<h3 class="card-title">With Mowing Bidder</h3>
			<ul>
				<li>Paste the address once</li>
				<li>Auto-calculated square footage</li>
				<li>Price ready before the next text</li>
			</ul>
		</div>
	</div>
</section>

<style>
	.hero {
		padding-top: 6rem;
	}

	.hero-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 3rem;
		align-items: center;
	}

	.hero-copy {
		display: grid;
		gap: 1.5rem;
	}

	.hero-actions {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.hero-metrics {
		display: flex;
		gap: 2rem;
		flex-wrap: wrap;
		font-size: 0.95rem;
		color: var(--muted);
	}

	.metric-label {
		display: block;
		font-size: 0.85rem;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		margin-bottom: 0.25rem;
	}

	.hero-card {
		background: var(--paper);
		border: 1px solid var(--line);
		border-radius: 1.8rem;
		padding: 2rem;
		box-shadow: var(--shadow);
		display: grid;
		gap: 1rem;
	}

	.hero-card ul {
		list-style: none;
		display: grid;
		gap: 0.65rem;
		color: var(--muted);
	}

	.card-title {
		font-weight: 700;
		font-size: 1.05rem;
		margin-bottom: 0.25rem;
	}

	@media (max-width: 600px) {
		.hero {
			padding-top: 4rem;
		}
	}
</style>
