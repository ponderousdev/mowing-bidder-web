<section class="social-proof">
	<div class="container proof-inner">
		<div>
			<p class="eyebrow">Built by lawn care operators</p>
			<h2 class="display">Trusted by crews who quote every day.</h2>
		</div>
		<ul class="proof-list">
			<li>Fewer back-and-forth texts</li>
			<li>Consistent pricing every time</li>
			<li>Faster quotes → more closed jobs</li>
		</ul>
	</div>
</section>

<style>
	.social-proof {
		background: rgba(255, 255, 255, 0.7);
		border-top: 1px solid var(--line);
		border-bottom: 1px solid var(--line);
	}

	.proof-inner {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: 2rem;
		align-items: center;
	}

	.proof-list {
		list-style: none;
		display: grid;
		gap: 0.75rem;
		font-weight: 600;
		color: var(--accent-dark);
	}
</style>
